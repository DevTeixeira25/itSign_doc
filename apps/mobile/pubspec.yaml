name: itsign_mobile
description: ITSign mobile app
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_auth: ^5.3.4
  firebase_core: ^3.8.1
  file_picker: ^8.1.6
  http: ^1.2.0
  pdf_render: ^1.4.12
  shared_preferences: ^2.2.0
  signature: ^5.4.1
  url_launcher: ^6.3.1

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
